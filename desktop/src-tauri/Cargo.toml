[package]
name = "colony-counter-desktop"
version = "0.1.0"
description = "Microbial Colony Counter desktop shell"
authors = ["you"]
edition = "2021"

[lib]
name = "colony_counter_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
